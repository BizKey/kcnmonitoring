services:
  kcnmonitoring:
    image: ghcr.io/bizkey/kcnmonitoring/kcnmonitoring:latest
    pull_policy: always
    container_name: kcnmonitoring
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - bridge
networks:
  bridge:
    driver: bridge